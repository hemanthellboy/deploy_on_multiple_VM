APP_NAME="loan-service"
JAR_NAME="loan-service-${VERSION}.jar"
TARGET_LIB_DIR="/opt/apps/loan-service/lib"
TARGET_CLASSES_DIR="/opt/apps/loan-service/classes"
EXTRA_ZIPS=( )
XML_PATH="$TARGET_CLASSES_DIR/WEB-INF/application.xml"
XML_EDITS=(
  # format: "xpath|newvalue"
  # Example: "/web-app/context-param[param-name='env']/param-value|production"
)
HEALTH_CHECKS=(
  "url:http://localhost:8080/health"
  "command:systemctl is-active loan-service >/dev/null 2>&1"
)
# Optional: provide artifacts/<filename>.sha256 or .asc to enable signature verification.
